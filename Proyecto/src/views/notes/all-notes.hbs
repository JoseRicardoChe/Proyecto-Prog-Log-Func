<div class="row">
  {{#each notes}}
  <div class="col-md-3">
    <div class="card">
      <div class="card-body">
        <h4 class="card-title d-flex justify-content-between align-items-center">
          {{ title }} <a href="/notes/edit/{{_id}}"><i class="fas fa-edit"></i></a>
        </h4>
        <p>{{ description }} </p>
        <!-- Para Borrar peticion -->
        <form action="/notes/delete/{{_id}}?_method=DELETE" method="POST">
          <input type="hidden" name="_method" value="DELETE">
          <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
        </form>
      </div>
    </div>
  </div>
  <!--recorre la targeta y no hay ninguna nota selecciona o redirecciona para crear de nuevo -->
  {{else}}
  <div class="card mx-auto">
    <div class="card-body">
      <h1>Hola {{user.name}}</h1>
      <p class="lead">Todavia no existe una Evidencia.</p>
      <a href="/notes/add" class="btn btn-success btn-block">Crear una nueva Evidencia!</a>
    </div>
  </div>
  {{/each}}
</div>